# Hello World

Welcome to my blog! This is a sample post written in MDX.

## Why MDX?

MDX allows you to write Markdown and use React components in your content.

```jsx
export function Hello() {
  return <p>Hello from MDX!</p>
}
```

You can also use your own components:

```tsx
import { Button } from '@/components/ui/button'

export default function Page() {
  return <Button>Click me</Button>
}
```

That's it for now!
